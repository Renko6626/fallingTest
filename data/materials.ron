// 材料表（P5：数据即确定性输入，内容哈希入指纹）。
// id 显式声明，禁依赖声明顺序；air 必须 id=0，wall 必须 id=1（core 校验）。
//
// hp + durability（M2 spec §2.2，Noita 双层破坏；原 blast_cost 改名 hp）：
// hp = 射线逐格能量消耗（能量池），durability = 破坏门槛——
// `durability > Op::Explode.max_durability`（场景 RON 缺省 10）即完全免疫、
// 射线断线。wall 取 durability 15（高于任何法术上限）替代原 u32::MAX 哨兵
// （BLAST_COST_INFINITE 已退役）：语义比"无限能量消耗"直白，且不再依赖
// "power 不会超过某个界"的隐含假设。无爆炸场景 golden 状态哈希逐位不变
// （2026-08-31 diff 取证），explosion_ci 因门槛语义变更全变（预期内）。
//
// vaporize_threshold（spec §6 汽化小节，用户裁决 2026-08-30）：爆炸射线剩余
// 能量比例（remaining/power）严格超过此阈值即汽化（删除、不生成粒子），做出
// "近心没了、外圈飞溅"的观感。RON 写 0.0..=1.0 十进制，加载期经
// `sand-harness::scenario::quantize_vaporize_threshold` 量化为 u8（×255
// round）；不写即缺省 1.0（永不汽化，量化后 255）。现值：water 0.4（量化
// 102，水更易被炸干净）、sand 0.95（量化 242，2026-08-30 用户目检从初值
// 0.7 两轮调参收窄汽化圈：0.7→0.9→0.95，沙以溅射为主）；air/wall 不写，
// 吃缺省（air 从不进入摧毁分支，wall 免疫爆炸，字段无意义）。同 blast_cost
// 先例：本文件内容哈希再次改变，全部 golden 的 materials_fp 行已重录；
// explosion_ci 因爆炸语义变更状态哈希全变，sand_pile/mixed/waterfall_ci
// 无爆炸，状态哈希逐位不变（重录前 diff 验证）。
// dispersion（Layer G Task 1，spec §3）：液体单 tick 横移格数，取值域
// [1, DISPERSION_MAX=8]，缺省 1 = 改动前的单格横移语义。这是 M0 记录的
// "水面锯齿 / 摊平极慢"顽疾的根因修复。现值：water 5（Noita 调研
// docs/reference/noita-deep-dive.md:242 参考值：水 ≈5、油更低、岩浆 1–2）；
// air/wall/sand 不写，吃缺省 1——粉末根本不走 side()（spec §1.3 Non-goals），
// Static 更不动，字段对它们无意义。
//
// **该字段不是手感旋钮而是 P4 写域论证的输入**：side() 的探测半径 = 写入
// 半径 = 色散距离，越界即写出 WriteWindow → 同相数据竞争。故两道防线：
// 加载期 `scenario::validate_dispersion` 给可读报错，core 侧 `rules::side`
// 用 DISPERSION_MAX clamp 兜死（spec §3.1 评审修订）。
//
// 本文件内容哈希（materials_fp）随之改变，全部 golden 的 materials_fp 行
// 已重录；含 water 的 mixed/waterfall_ci/explosion_ci 状态哈希全变（语义
// 变更，预期内），sand_pile 无 water，逐 tick 哈希逐位不变（重录前已用
// `hashrun --grid-only` diff 取证）。
// splash_chance（Layer G Task 3，spec §6.2）：本 tick 撞停且速度达
// SPLASH_MIN_SPEED（2.0 格/tick）时，按此概率把该格脱格成一颗向上飞的粒子
// （撞击水花）。RON 写 0.0..=1.0 十进制，加载期经
// `sand-harness::scenario::quantize_splash_chance` 量化为 u8（×255 round）；
// 不写即缺省 0.0 = 永不溅射，故未声明该字段的材质行为逐位不变。
// 现值：water 0.6（量化 153，瀑布砸地的水花是本 Task 的主要目标）、
// sand 0.1（量化 26，沙以堆积为主，少量迸溅）；air/wall 不写吃缺省
// （air 不会撞停，wall 是 Static 根本不进 eval）。
//
// 与 dispersion 不同，本字段**不进** P4 写域论证——溅射产出的是粒子，走
// Layer P 自己的 DDA 与串行落格，不经 WriteWindow。故沿用 blast_cost /
// vaporize_threshold 的"core 侧不校验"先例。
//
// 本文件内容哈希（materials_fp）随之改变，全部 golden 已重录。
(
    materials: [
        (id: 0, name: "air",   category: Static, density: 0,   color: (16, 16, 24),    hp: 0),
        (id: 1, name: "wall",  category: Static, density: 100, color: (110, 106, 118), hp: 100, durability: 15),
        (id: 2, name: "sand",  category: Powder, density: 40,  color: (214, 176, 96),  hp: 2, vaporize_threshold: 0.95, splash_chance: 0.1),
        (id: 3, name: "water", category: Liquid, density: 16,  color: (48, 108, 220),  hp: 1, vaporize_threshold: 0.4, dispersion: 5, splash_chance: 0.6),
        // ---------- M2 材质最小集（spec §1.3，用户裁决 2026-08-31）----------
        // Task 1 先落材质本体与 tag；燃烧参数（fire_hp/lifetime/fire_temp/
        // fire_chance/extinguisher）随 Task 3 一并声明——Task 1 里没人消费它们，
        // 先给会让"golden 只变 fp 行"的逐位取证掺进无意义的噪声。
        // oil：可燃液体，密度低于水（浮油）；色散取 2（Noita 参考：油低于水，
        // noita-deep-dive.md:242）。
        (id: 4, name: "oil",   category: Liquid, density: 12,  color: (96, 78, 40),   hp: 1, dispersion: 2, tags: ["burnable"]),
        // wood：可燃 Static，未点燃时与 wall 同样零成本退出 eval。
        (id: 5, name: "wood",  category: Static, density: 60,  color: (122, 86, 48),  hp: 3, tags: ["burnable"]),
        // fire/smoke：Gas，上浮 + 水平扩散恒 1 格（不声明 dispersion，加载期
        // 校验禁止；spec §3.1）。fire 密度最低——烟从火焰里穿升不合理。
        (id: 6, name: "fire",  category: Gas,    density: 1,   color: (236, 120, 24), hp: 0),
        (id: 7, name: "smoke", category: Gas,    density: 2,   color: (70, 70, 76),   hp: 0),
    ],
)
